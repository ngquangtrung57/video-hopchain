# Video-HopChain training

A fork of verl that adds Second-Wave Exploration (SWE) to GRPO, plus the reward function for
Video-HopChain. Launch with `scripts/train/hopchain/launch_swe.sh` or `launch_plain.sh`.

See the repository README for the topology, the environment variables and the SWE settings.
