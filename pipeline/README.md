# Video-HopChain pipeline

Turns raw video into multi-hop questions with verifiable integer answers. One entry point:

```bash
python run.py --videos <dir> --out <dir> --caption-model <name> --text-model <name>
```

See the repository README for the ten stages, the flags and the environment.
